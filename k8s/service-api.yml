apiVersion: v1
kind: Service
metadata:
  name: api-service-gestaoacesso
spec:
  type: NodePort
  selector:
    app: api-gestaoacesso
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 80
      nodePort: 30000